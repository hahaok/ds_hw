#include <stdio.h>
#include <math.h>>

int main()
{
    int num;
    scanf("%d",&num);
    int temp1 = 2,temp2;
    for(int i =3;i <= num; i++)
    {
        for(int j = 2;j < (int)(sqrt(num)); j++)
        {

        }
    }
    return 0;
}
